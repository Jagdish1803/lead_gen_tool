import { ComingSoon } from "@/components/coming-soon";

export default function SettingsPage() {
  return (
    <ComingSoon title="Settings" phase="Phase 4+">
      API keys, the WhatsApp number/session, and the message-pacing controls
      (delays, daily cap) will live here.
    </ComingSoon>
  );
}
